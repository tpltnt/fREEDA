%
% Indexes
%
% To make indexes
% Run entire document through latex
% Copy spice.idx to indexes.idx
% Organize indexes.idx appropriately (i.e. sort, and group)
% Then includeonly{index} and rerun
%
\newcommand{\indexentry}[2]{#1, #2\\}
